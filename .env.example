# Kapital Bank API Credentials
# Copy this file to .env and fill in your actual values

# XSRF Token from browser request headers
# Find this in: DevTools -> Network -> Request Headers -> xsrf-token
export XSRF_TOKEN="your_xsrf_token_here"

# Full cookie string from browser request headers
# Find this in: DevTools -> Network -> Request Headers -> cookie
export KB_COOKIES="your_full_cookie_string_here"

# OpenAI API Key for AI parsing of receipts
# Get your API key from: https://platform.openai.com/api-keys
export OPENAI_API_KEY="sk-proj-your_openai_api_key_here"

# How to get these values:
# 1. Open https://edvgerial.kapitalbank.az/az/dashboard in your browser
# 2. Open Developer Tools (F12)
# 3. Go to Network tab
# 4. Refresh the page or trigger a request to /api/v1/cashback/history
# 5. Click on the request and find the Request Headers section
# 6. Copy the xsrf-token value
# 7. Copy the entire cookie string